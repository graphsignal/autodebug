# debug-2026-03-30T21:10:00Z

**Config changes from previous**: `--num-continuous-decode-steps 8`, `--enable-mixed-chunk`, `--schedule-policy lpm`

**Metrics** (server-side, sequential 31 prompt / 150 completion tokens):
TTFT≈5ms (30 cached tokens → 1 new token prefill), TBOT=1.42ms/token, TTLT≈215ms, Errors=0
prefill_throughput=N/A (cached), decode_throughput=704 tok/s, token_throughput≈698 tok/s

**Analysis** (from Graphsignal debug context):

- Prometheus metric `sglang.gen_throughput`: **729.50 tok/s** at 21:02:32 → up from baseline 650-675 tok/s (+9% decode throughput)
- Prometheus metric `sglang.inter_token_latency_seconds`: **1.42ms/token** at 21:02:38 → down from baseline 1.50ms (-5.3% TBOT improvement)
- Prometheus metric `sglang.time_to_first_token_seconds`: 1.21s cumulative avg at 21:02:38 (dropping fast — subsequent requests hit 30 cached tokens)
- Server log: "Prefill batch, #new-seq: 1, #new-token: 1, #cached-token: 30" on requests 2-5 → LPM schedule policy causing 30/31 tokens to hit radix cache
- Server log: "Prefill batch, #new-seq: 1, #new-token: 13, #cached-token: 14" on first haiku → partial prefix hit from prior warmup
- Server log: "gen throughput (token/s): 701.62, 728.83, 727.22, 698.12, 697.13" → sustained ~720 tok/s decode (vs 640-675 baseline)
- Server log: "Decode batch, #token: 33, gen throughput: 48.55" for first decode step → CUDA graph initialization overhead on first batch (same as baseline)
- Parallel benchmark client TTFT: 0.64s (first) vs 3.37s (remaining 4) → server logs confirm staggered arrivals (21:02:36 for req 1, 21:02:38 for reqs 2-5); **not a scheduler regression** — caused by network/proxy queuing at dstack gateway
- GPU utilization: at 729 tok/s × 1B FLOPs/token (0.5B model) = 729 GFLOP/s vs H100 989 TFLOP/s BF16 → **0.07% GPU utilization** — extremely memory/dispatch bound for this tiny model
- Span: warmup span duration=116ms (TTFT=116ms, 20 tokens, 0 cached); subsequent span=23ms (TTFT=23ms, 20 tokens, 14 cached) — confirms radix cache dramatically reducing TTFT

**Findings**:
- `num_continuous_decode_steps=8` improved decode throughput by +9% and TBOT by -5.3% — confirmed effective
- `schedule_policy=lpm` enabling high prefix cache utilization (30/31 tokens cached)
- GPU is near-idle for this model size; bottleneck is CPU-side scheduling overhead and kernel dispatch
- Per-token cost breakdown: ~0.66ms model forward + ~1.07ms scheduler → scheduler overhead is 62% of total TBOT
- To make meaningful progress: need more tokens per GPU kernel invocation (speculative decoding) or reduce kernel dispatch count (torch.compile)

**Planned changes**:
1. Add `--speculative-algorithm ngram --speculative-num-draft-tokens 3 --speculative-num-steps 3` — n-gram speculation requires no draft model; at 40-60% acceptance rate, effective tokens per forward pass rises from 1 to ~2.2-2.8, targeting 2x TBOT improvement
2. Increase `--num-continuous-decode-steps 16` (from 8) — further amortize 1ms scheduler overhead per step; at 16 steps effective scheduler cost drops to 0.066ms/token
